import {Route} from '@angular/router';
import {RestClientComponent} from "./rest-client.component";

export const RestClientRoutes:Route[] = [
    {
        path: 'restclient',
        component: RestClientComponent
    }
];
